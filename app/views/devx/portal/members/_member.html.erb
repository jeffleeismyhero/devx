<tr>
  <td><%= link_to member.try(:full_name), devx.edit_portal_member_path(member) %></td>
  <td><%= member.position %></td>
  <td>
    <%= link_to 'Edit', devx.edit_portal_member_path(member) %>
    <%= link_to 'Delete', devx.portal_member_path(member), method: :delete, data: { confirm: "Are you sure you want to delete #{member.try(:full_name)}?" } %>
  </td>
</tr>