<tr>
    <td style="padding:10px;border-bottom:1px solid #ccc;"><%= link_to stylesheet.name, devx.edit_portal_stylesheet_path(stylesheet) %></td>
    <td style="text-align:right; padding:10px;border-bottom:1px solid #ccc;">
        <%= link_to 'Edit', devx.edit_portal_stylesheet_path(stylesheet) %> <i class="fa fa-pencil-square-o" aria-hidden="true"></i>
        <%= link_to 'Delete', devx.portal_stylesheet_path(stylesheet), method: :delete, data: { confirm: "Are you sure you want to delete #{stylesheet.name}?" } %>  <i class="fa fa-trash" aria-hidden="true"></i>
    </td>
</tr>
