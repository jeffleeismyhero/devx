<% provide(:title, '' + controller_name.classify.pluralize) %>

<div class="container">
    <div class="row">
        <div class="col-12 columns">
            <h1><%= yield(:title) %></h1>
        </div>
        <div class="col-12 columns" style="padding-bottom:20px;">
            <%= form_for @import, url: devx.portal_users_import_path, method: :post do |f| %>
                <%= f.label :file %>
                <%= f.file_field :file %>

                <div class="col-12 columns" style="margin-top:20px;">
                    <div class="cd-update-form">
                        <%= submit_tag %>
                    </div>
                </div>
            <% end %>
        </div>
    </div>
</div>