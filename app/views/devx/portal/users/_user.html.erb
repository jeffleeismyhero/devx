<tr>
    <td style="padding:10px;border-bottom:1px solid #ccc;"><%= link_to user.email, devx.edit_portal_user_path(user) %></td>
    <td style="padding:10px;border-bottom:1px solid #ccc;"><%= user.first_name %></td>
    <td style="padding:10px;border-bottom:1px solid #ccc;"><%= user.last_name %></td>
    <td style="text-align:right; padding:10px;border-bottom:1px solid #ccc;">
    <%= link_to 'Edit', devx.edit_portal_user_path(user) %>
    <%= link_to 'Delete', devx.portal_user_path(user), method: :delete, data: { confirm: "Are you sure you want to delete #{user.email}?" } %>
    </td>
</tr>