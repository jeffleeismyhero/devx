<tr>
    <td style="padding:10px;border-bottom:1px solid #ccc;"><%= order.id %></td>
    <td style="padding:10px;border-bottom:1px solid #ccc;"><%= order.created_at.try(:strftime, '%m/%d/%Y %I:%M %P') %></td>
    <td style="padding:10px;border-bottom:1px solid #ccc;"><%= order.user.try(:full_name) %></td>
    <td style="text-align:right; padding:10px;border-bottom:1px solid #ccc;">
    <%= link_to 'View', devx.portal_order_path(order) %> <i class="fa fa-pencil-square-o" aria-hidden="true"></i>
    <%= link_to 'Delete', devx.portal_order_path(order), method: :delete, data: { confirm: "Are you sure you want to delete Order ##{order.id}?" } %> <i class="fa fa-trash" aria-hidden="true"></i>
    </td>
</tr>
