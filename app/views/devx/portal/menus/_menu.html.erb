<tr>
    <td><%= link_to menu.name, devx.edit_portal_menu_path(menu) %></td>
    <td>
        <%= link_to 'Edit', devx.edit_portal_menu_path(menu) %> <i class="fa fa-pencil-square-o" aria-hidden="true"></i>
        <%= link_to 'Delete', devx.portal_menu_path(menu), method: :delete, data: { confirm: "Are you sure you want to delete #{menu.name}?" } %>  <i class="fa fa-trash" aria-hidden="true"></i>
    </td>
</tr>
