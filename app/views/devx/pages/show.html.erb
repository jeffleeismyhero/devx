<% provide(:title, @page.name) %>
<% @shortcodes.try(:each) do |shortcode| %>
  <% @page.content.gsub!("#{shortcode}", sc_content_html(shortcode)) %>
<% end %>
<%= Devx::Compressor.compress(@page.content.html_safe) %>